name: Apply playback queue revision fix

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'agent/fix-playback-panel-json'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Apply queue revision patch
        shell: bash
        run: |
          python3 .github/scripts/apply_playback_revision_fix.py
          rm .github/scripts/apply_playback_revision_fix.py
          rm .github/workflows/apply-playback-revision-fix.yml
          git diff --check

      - name: Commit queue revision fix
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Use playback queue revision for content changes"
          git push origin HEAD:${{ github.head_ref }}
