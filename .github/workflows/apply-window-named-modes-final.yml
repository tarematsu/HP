name: Validate named window modes

on:
  push:
    branches:
      - feat/window-named-audio-monitor-modes
    paths:
      - .github/workflows/apply-window-named-modes-final.yml

permissions:
  contents: write

jobs:
  validate-and-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: hp/package-lock.json
      - name: Install HomePanel dependencies
        working-directory: hp
        run: npm ci --prefer-offline --no-audit --no-fund
      - name: Apply implementation and regression fixes
        run: |
          python .github/scripts/apply_window_named_modes.py
          python .github/scripts/finalize_window_named_modes.py
      - name: Check video workspace
        working-directory: hp
        run: npm run check --workspace video
      - name: Test video workspace
        working-directory: hp
        run: npm test --workspace video
      - name: Commit implementation
        shell: bash
        run: |
          git rm .github/workflows/apply-window-named-modes.yml
          git rm .github/workflows/apply-window-named-modes-v2.yml
          git rm .github/workflows/apply-window-named-modes-v3.yml
          git rm .github/workflows/apply-window-named-modes-final.yml
          git rm .github/workflows/capture-window-mode-tests.yml
          git rm .github/scripts/apply_window_named_modes.py
          git rm .github/scripts/finalize_window_named_modes.py
          git rm .github/window-mode-test-output.txt
          git rm .github/window-mode-test-status.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add hp/native/src hp/video/test
          git commit -m "Use window names for Stationhead audio and monitor modes"
          git push origin HEAD:feat/window-named-audio-monitor-modes
