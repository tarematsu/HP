name: Video CI

on:
  pull_request:
    paths:
      - 'hp/video/**'
      - '.github/workflows/video-ci.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-stationhead-ab-audit:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo paused
