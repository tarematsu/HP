#pragma once

// Retired compatibility header.
// Blank-page recovery now belongs to StationheadCompactRuntimeScript as two
// bounded one-shot checks. Keeping this header empty avoids a second recovery
// scheduler while older include lists are being phased out.
