#pragma once

// Retired compatibility header.
// WebView2 native audio state plus the existing native track-boundary recovery
// own playback liveness. The former 15-second page media watchdog duplicated
// that state and is intentionally not installed anymore.
