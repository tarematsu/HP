# Direct native source build

The native build compiles checked-in sources directly.

Do not add build-time source patching or source-generation steps back for application behavior changes. Edit the checked-in source files instead.
